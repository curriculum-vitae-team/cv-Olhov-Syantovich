import styled from '@emotion/styled';

export const Title = styled('h1')({
  color: 'rgba(255, 255, 255, 1)',
  textAlign: 'center',
  fontSize: '30px',
  fontFamily: 'Poppins',
  fontWeight: '800'
});
